// jshint esversion: 6

describe('Query', () => {
    describe('#tokens', () => {
        it('should return an array of weighted tokens', () => {
            var q = new Query('Delon & Jackie Newman (2)');
            var tokens = [{ token: 'd', weight: 2 }];

            expect();
        });
    });
});